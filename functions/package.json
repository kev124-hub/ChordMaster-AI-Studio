{
  "name": "chordmaster-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions backend for ChordMaster AI Studio",
  "main": "lib/index.js",
  "engines": { "node": "20" },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "@google/genai": "^1.29.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "typescript": "~5.8.2",
    "@types/node": "^20.0.0"
  }
}
